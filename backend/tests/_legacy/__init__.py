# Tests for the legacy Tiptap/DOCX/HTML-preview build. Skipped by
# pytest during Phase 1A; kept for reference until the fixed-PDF
# marker approach is fully accepted.
import pytest
collect_ignore_glob = ["*"]
